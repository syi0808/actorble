# Actorble Browser Follow-up Tasks - 2026-06-14

이 문서는 `docs/implementation_tasks.md`와 2026-06-14 현재 `browser/` 패키지 상태를 대조해 남은 후속 작업을 정리한다. T0-T40의 core vertical slice는 대체로 구현되어 있으며 `click`, `moveTo`, `typeInto`, visual runtime, scenario delay/pacing 같은 흐름은 테스트로 보호되어 있다. 다만 public API shell, 일부 선언형 조건, scenario action 범위, smoke 검증에는 아직 명시적인 후속 작업이 남아 있다.

## 현재 검증 상태

관찰 시점 기준 검증 결과:

```txt
pnpm test              -> pass, 23 files / 225 tests
pnpm typecheck         -> pass
pnpm build             -> pass
pnpm example:typecheck -> pass
pnpm example:build     -> pass
pnpm example:smoke     -> fail
```

`pnpm example:smoke`는 `scripts/action-playground-smoke.mjs`에서 `#run-current` 클릭 후 `#run-status`가 기대 상태로 바뀌기를 기다리다가 timeout 된다. 예제 UI는 새 task scenario 형태로 바뀌었고, smoke script도 일부 정렬되어 있으나 실제 flow 완료 검증은 아직 안정화되지 않았다.

## 의존성 원칙

- Facade는 public API 위임과 composition root만 담당한다.
- Action Orchestrator는 action lifecycle을 조율하고 DOM/event 직접 작업은 platform adapter와 input engine에 맡긴다.
- Geometry 계산과 interactability 판단은 계속 분리한다.
- Pointer/Gesture 계층은 좌표, 버튼, signal sequence를 담당하고 target 의미 상태는 Interaction State Store와 Orchestrator 경계에서 다룬다.
- Browser 한계는 숨기지 않고 capability/fidelity report 또는 `PLATFORM_UNSUPPORTED`로 드러낸다.
- 예제/smoke 작업은 public API만 사용하고 내부 concrete tracker/orchestrator state에 의존하지 않는다.

## 의존성 맵

```txt
example smoke
  -> example/action-playground
  -> actorble-facade public API

actorble-facade
  -> action-orchestrator
  -> target-resolver
  -> geometry-engine
  -> scenario-runner
  -> diagnostics-trace

action-orchestrator
  -> target-resolver
  -> surface-engine
  -> geometry-engine
  -> interactability-engine
  -> gesture-engine
  -> focus-engine
  -> keyboard-engine
  -> text-input-engine
  -> wait-observation-engine
  -> diagnostics-trace

scenario-runner
  -> action-orchestrator
  -> timeline-engine
  -> diagnostics-trace

wait-observation-engine
  -> target-resolver or narrow target wait port
  -> geometry/interactability where needed
  -> timeline-engine
  -> diagnostics-trace

surface-engine
  -> platform-adapter/dom-adapter
  -> shared coordinate primitives

gesture-engine
  -> pointer-engine
  -> pointer-signals
  -> timeline-engine
```

## 작업 순서

### F2026-06-14-01A Smoke Quiet Flow Recovery

- Status: [x] Completed
- Briefing: `pnpm example:smoke`를 현재 task scenario 예제의 기본 quiet flow와 다시 맞춘다. 현재 실패는 selector 부재가 아니라 `#run-current` 실행 후 기대 status에 도달하지 못하는 문제다.
- Dependencies: `example/action-playground`, `scripts/action-playground-smoke.mjs`, public `createActorble()` flow.
- Completion criteria:
  - action playground smoke가 GitHub/Form/Search scenario 중 최소 기본 quiet flow를 완료한다.
  - 실패 시 `#run-status`, trace, event log 중 어디에서 멈췄는지 확인 가능한 diagnostics를 남긴다.
  - smoke script는 내부 concrete tracker/orchestrator state에 의존하지 않고 public API와 예제 DOM만 관찰한다.
- Test expectations:
  - `pnpm example:smoke`
  - `pnpm example:typecheck`
  - `pnpm example:build`

### F2026-06-14-01B Visual Mode Smoke Matrix

- Status: [x] Completed
- Briefing: smoke 기본 flow가 복구된 뒤 quiet/debug/off visual mode의 hit-test 방해 여부를 현재 예제 UI 구조와 맞춰 검증한다.
- Dependencies: 완료된 F2026-06-14-01A, `example/action-playground`, `scripts/action-playground-smoke.mjs`, visual runtime.
- Completion criteria:
  - quiet mode는 cursor-only visual feedback 상태에서 scenario flow를 방해하지 않는다.
  - debug mode는 highlight/click/focus/typing visual feedback이 켜져도 target hit-test를 방해하지 않는다.
  - visual off mode는 overlay DOM 없이 같은 scenario flow를 완료한다.
- Test expectations:
  - `pnpm example:smoke`
  - visual mode별 smoke 옵션을 추가하면 해당 옵션 경로를 함께 실행한다.

### F2026-06-14-02A Facade Delegation For Supported Actions

- Status: [x] Completed
- Briefing: `src/api/actorble-facade/index.ts`에 남은 orchestrator-owned public API shell을 injected orchestrator로 단순 위임한다. default orchestrator가 아직 unsupported를 반환하는 action도 facade에서는 직접 action lifecycle을 수행하지 않는다.
- Dependencies: `actorble-facade`, `action-orchestrator`, `geometry-engine`, `diagnostics-trace`.
- Completion criteria:
  - orchestrator-owned public methods는 facade에서 `notImplemented()`를 호출하지 않고 injected orchestrator로 위임된다.
  - facade는 action lifecycle을 직접 수행하지 않고 injected orchestrator/resolver/runner/geometry/trace에만 위임한다.
  - facade 단위 테스트가 각 위임 대상과 option 전달을 검증한다.
- Test expectations:
  - `pnpm test -- tests/actorble-facade.test.js`
  - `pnpm typecheck`

### F2026-06-14-02B Unsupported Public API Reporting

- Status: [x] Completed
- Briefing: 아직 구현하지 않을 public API는 silent fallback이나 generic shell 대신 capability/fidelity와 같은 한계를 설명하는 명시적 unsupported path로 정리한다.
- Dependencies: `actorble-facade`, `action-orchestrator`, `capability-fidelity`, `shared` error helpers.
- Completion criteria:
  - 남겨진 unsupported public method는 `NOT_IMPLEMENTED` shell이 아니라 의도한 `PLATFORM_UNSUPPORTED` 또는 좁은 error code를 사용한다.
  - error details는 boundary, action, capability limit을 확인할 수 있다.
  - capability/fidelity report와 error message가 서로 모순되지 않는다.
- Test expectations:
  - `pnpm test -- tests/actorble-facade.test.js tests/capability-fidelity.test.js`
  - `pnpm typecheck`

### F2026-06-14-03A Orchestrator Focus Public Path

- Status: [ ] Not started
- Briefing: `focus(target)`를 resolve/reveal/preflight/focus/wait/cleanup lifecycle로 연결해 Focus Engine을 public path에서 사용할 수 있게 한다.
- Dependencies: `action-orchestrator`, `focus-engine`, `interactability-engine`, `surface-engine`, `wait-observation-engine`, `interaction-state-store`.
- Completion criteria:
  - `focus(target)`는 target resolve와 stale validation 이후 focusable preflight를 수행한다.
  - focus 성공 후 wait/settle과 focus visual feedback cleanup이 action lifecycle에 포함된다.
  - cancellation/timeout 시 focus visual state가 남지 않는다.
- Test expectations:
  - `pnpm test -- tests/action-orchestrator.test.js tests/focus-keyboard-text-input.test.js`
  - `pnpm typecheck`

### F2026-06-14-03B Orchestrator Type Current Focus Path

- Status: [ ] Not started
- Briefing: `type(text)`를 현재 focused editable target에만 동작하는 public path로 연결한다. 이 action은 target click이나 locator resolve를 자동으로 수행하지 않는다.
- Dependencies: 완료된 F2026-06-14-03A, `action-orchestrator`, `text-input-engine`, `focus-engine`, `wait-observation-engine`, `interaction-state-store`.
- Completion criteria:
  - focused editable target이 없으면 actionable error를 낸다.
  - `type(text)`는 Text Input Engine의 current focus path를 사용하고 `typeInto`와 target resolve 책임을 섞지 않는다.
  - cancellation/timeout 시 typing visual state와 store typing slice가 cleanup된다.
- Test expectations:
  - `pnpm test -- tests/action-orchestrator.test.js tests/focus-keyboard-text-input.test.js`
  - `pnpm typecheck`

### F2026-06-14-03C Orchestrator Fill Target Path

- Status: [ ] Not started
- Briefing: `fill(target, text)`를 target resolve와 값 대체 전략으로 연결하고, cadence 기반 `typeInto`와 의미를 분리한다.
- Dependencies: 완료된 F2026-06-14-03A, `action-orchestrator`, `text-input-engine`, `interactability-engine`, `wait-observation-engine`.
- Completion criteria:
  - `fill(target, text)`는 resolve/reveal/preflight/fill/wait/cleanup lifecycle을 가진다.
  - fill은 값 대체 전략을 사용하고 per-character typing cadence를 사용하지 않는다.
  - failure/cancellation 시 typing visual state가 cleanup된다.
- Test expectations:
  - `pnpm test -- tests/action-orchestrator.test.js tests/focus-keyboard-text-input.test.js`
  - `pnpm typecheck`

### F2026-06-14-03D Orchestrator Press Path

- Status: [ ] Not started
- Briefing: `press(keys)`를 Keyboard Engine에 연결해 keyDown/keyUp/press state와 modifiers를 public action lifecycle에서 관리한다.
- Dependencies: `action-orchestrator`, `keyboard-engine`, `focus-engine`, `wait-observation-engine`, `interaction-state-store`.
- Completion criteria:
  - `press(keys)`는 keyboard engine을 통해 dispatch되고 modifier state cleanup이 보장된다.
  - focused target이 필요한 키 입력 실패가 trace에서 phase와 함께 드러난다.
  - cancellation/timeout 시 pressed modifier state가 남지 않는다.
- Test expectations:
  - `pnpm test -- tests/action-orchestrator.test.js tests/focus-keyboard-text-input.test.js`
  - `pnpm typecheck`

### F2026-06-14-04A DoubleClick Gesture And Public Path

- Status: [ ] Not started
- Briefing: `doubleClick(target)`을 Gesture Engine의 multi-click sequence와 orchestrator action lifecycle로 연결한다.
- Dependencies: `action-orchestrator`, `gesture-engine`, `pointer-engine`, `pointer-signals`, `interactability-engine`, `diagnostics-trace`.
- Completion criteria:
  - `doubleClick(target)`은 move/preflight 후 두 번의 down/up/click sequence를 dispatch한다.
  - `clickCount > 1`이 generic unsupported path로만 떨어지지 않는다.
  - visual press dwell과 click feedback은 single click 책임과 섞이지 않는다.
- Test expectations:
  - `pnpm test -- tests/gesture-engine.test.js tests/action-orchestrator.test.js tests/actorble-facade.test.js`

### F2026-06-14-04B ClickCurrent Current Target Policy

- Status: [ ] Not started
- Briefing: pointer가 마지막으로 가리킨 target 또는 point를 기반으로 `clickCurrent()`의 최소 정책을 정하고 public path로 연결한다.
- Dependencies: 완료된 F2026-06-14-04A, `action-orchestrator`, `pointer-engine`, `pointer-signals`, `interaction-state-store`, `target-resolver`.
- Completion criteria:
  - `clickCurrent()`는 명확한 current target/point가 없을 때 actionable error를 낸다.
  - current target이 stale/detached이면 validate 실패가 trace와 error details에 남는다.
  - current click은 target resolve 없이도 기존 pointer state를 사용하되 dispatch 전 interactability preflight를 수행한다.
- Test expectations:
  - `pnpm test -- tests/action-orchestrator.test.js tests/actorble-facade.test.js`

### F2026-06-14-05A ScrollTo Target Public Path

- Status: [ ] Not started
- Briefing: `scrollTo(targetLike)` facade/orchestrator shell을 제거하고 locator resolve 후 Surface Engine으로 위임하는 public path를 만든다.
- Dependencies: `actorble-facade`, `action-orchestrator`, `surface-engine`, `target-resolver`, `wait-observation-engine`.
- Completion criteria:
  - `scrollTo(targetLike)`는 locator를 resolve하고 target freshness를 validate한 뒤 Surface Engine으로 위임한다.
  - scroll 후 settle과 layout invalidation diagnostics가 action trace에 연결된다.
  - unsupported locator or stale target failure가 target context와 함께 반환된다.
- Test expectations:
  - `pnpm test -- tests/surface-engine.test.js tests/action-orchestrator.test.js tests/actorble-facade.test.js`
  - `pnpm typecheck`

### F2026-06-14-05B ScrollTo Position And Coordinate Policy

- Status: [ ] Not started
- Briefing: `scrollTo(position)`의 지원 좌표계와 미지원 좌표계를 명확히 구분하고 필요한 최소 viewport/document 변환 정책을 정한다.
- Dependencies: 완료된 F2026-06-14-05A, `surface-engine`, `platform-adapter/dom-adapter`, shared coordinate primitives.
- Completion criteria:
  - `scrollTo(position)`은 지원 좌표계에서 viewport scroll target으로 위임된다.
  - 미지원 coordinate space는 `PLATFORM_UNSUPPORTED` details에 coordinate space를 남긴다.
  - `mapPoint()`는 실제 지원하는 변환만 통과시키고 나머지는 문서화된 제한으로 남긴다.
- Test expectations:
  - `pnpm test -- tests/surface-engine.test.js tests/action-orchestrator.test.js tests/actorble-facade.test.js`
  - `pnpm typecheck`

### F2026-06-14-06A Declarative Visible And Hidden Waits

- Status: [ ] Not started
- Briefing: `WaitCondition`의 `visible`과 `hidden`을 observable behavior로 구현한다.
- Dependencies: `wait-observation-engine`, target wait port 또는 `target-resolver`, `geometry-engine`, `interactability-engine`, `timeline-engine`, `diagnostics-trace`.
- Completion criteria:
  - `visible(target)`는 resolve/validate 후 visibility 또는 interactability boundary를 사용해 판단한다.
  - `hidden(target)`는 not found, detached, hidden 상태를 명확히 구분한다.
  - timeout/cancellation diagnostics가 custom predicate path와 같은 수준으로 남는다.
- Test expectations:
  - `pnpm test -- tests/wait-observation-engine.test.js`
  - action lifecycle 경유 테스트가 필요하면 `tests/action-orchestrator.test.js` 추가

### F2026-06-14-06B Declarative Text Wait

- Status: [ ] Not started
- Briefing: `WaitCondition`의 `text(value)`를 root text observation으로 구현하고 target-scoped 확장 지점은 별도 한계로 남긴다.
- Dependencies: 완료된 F2026-06-14-06A, `wait-observation-engine`, `platform-adapter/dom-adapter`, `timeline-engine`, `diagnostics-trace`.
- Completion criteria:
  - `text(value)`는 root text content를 반복 관찰해 string/RegExp 조건을 만족시킨다.
  - target-scoped text wait이 아직 없다면 error/capability details에 확장 지점을 남긴다.
  - timeout/cancellation diagnostics가 condition summary와 attempts를 포함한다.
- Test expectations:
  - `pnpm test -- tests/wait-observation-engine.test.js`

### F2026-06-14-07A Scenario Focus Step

- Status: [ ] Not started
- Briefing: Scenario Runner의 step union과 runtime dispatch에 `focus` step을 추가한다.
- Dependencies: 완료된 F2026-06-14-02A, 완료된 F2026-06-14-03A, `scenario-runner`.
- Completion criteria:
  - `focus` step이 target/options를 검증하고 orchestrator `focus`에 위임된다.
  - unsupported step error는 유지하되 `focus`는 더 이상 unsupported로 처리하지 않는다.
  - scenario timeout, external abort, `stop()`이 focus step에도 전파된다.
- Test expectations:
  - `pnpm test -- tests/scenario-runner.test.js`
  - `pnpm typecheck`

### F2026-06-14-07B Scenario Type Fill Press Steps

- Status: [ ] Not started
- Briefing: Scenario Runner에 `type`, `fill`, `press` step을 추가해 기본 keyboard/text action을 선언형 scenario에서 사용할 수 있게 한다.
- Dependencies: 완료된 F2026-06-14-03B, 완료된 F2026-06-14-03C, 완료된 F2026-06-14-03D, `scenario-runner`.
- Completion criteria:
  - `type`, `fill`, `press` step은 필요한 input/target/options를 검증하고 orchestrator에 위임된다.
  - 새 step에도 scenario timeout, external abort, `stop()`이 동일하게 전파된다.
  - trace에서 step action, input summary, cancellation/failure phase가 식별된다.
- Test expectations:
  - `pnpm test -- tests/scenario-runner.test.js`
  - public scenario example을 건드리면 `pnpm example:typecheck`

### F2026-06-14-07C Scenario ScrollTo Step

- Status: [ ] Not started
- Briefing: Scenario Runner에 `scrollTo` step을 추가해 target scroll과 position scroll을 선언형 scenario에서 사용할 수 있게 한다.
- Dependencies: 완료된 F2026-06-14-05A, 완료된 F2026-06-14-05B, `scenario-runner`.
- Completion criteria:
  - `scrollTo` step은 target 또는 position input을 검증하고 orchestrator `scrollTo`에 위임된다.
  - unsupported coordinate or malformed input error는 step index/action details를 포함한다.
  - scenario timeout, external abort, `stop()`이 scroll step에도 동일하게 전파된다.
- Test expectations:
  - `pnpm test -- tests/scenario-runner.test.js`
  - public scenario example을 건드리면 `pnpm example:typecheck`

### F2026-06-14-08A Pointer Drag Gesture Sequence

- Status: [ ] Not started
- Briefing: Gesture Engine에 synthetic pointer drag의 최소 sequence를 추가한다. HTML5/native DnD는 이 태스크 범위 밖으로 둔다.
- Dependencies: `gesture-engine`, `pointer-engine`, `pointer-signals`, `timeline-engine`.
- Completion criteria:
  - pointer drag가 move/down/move/up sequence를 수행한다.
  - cancel/failure 시 pressed pointer state가 cleanup된다.
  - HTML5 drag/drop, editor selection, native trusted DnD는 계속 별도 capability로 남는다.
- Test expectations:
  - `pnpm test -- tests/gesture-engine.test.js`

### F2026-06-14-08B Orchestrator Drag Public Path

- Status: [ ] Not started
- Briefing: `drag(from, to)`를 start/end target resolve, geometry refresh, interactability preflight, gesture drag, wait/cleanup lifecycle로 연결한다.
- Dependencies: 완료된 F2026-06-14-08A, `action-orchestrator`, `target-resolver`, `geometry-engine`, `interactability-engine`, `wait-observation-engine`.
- Completion criteria:
  - drag는 start/end target을 resolve하고 dispatch 직전 geometry를 refresh한다.
  - drag failure/cancellation 시 pressed/dragging visual state cleanup이 보장된다.
  - native DnD가 아닌 synthetic pointer drag임이 trace output에 드러난다.
- Test expectations:
  - `pnpm test -- tests/action-orchestrator.test.js tests/gesture-engine.test.js`
  - `pnpm typecheck`

### F2026-06-14-08C Drag Capability And Fidelity Update

- Status: [ ] Not started
- Briefing: synthetic pointer drag가 구현된 뒤 capability/fidelity report를 실제 지원 수준으로 갱신한다.
- Dependencies: 완료된 F2026-06-14-08B, `capability-fidelity`.
- Completion criteria:
  - capability report의 drag/drop 값이 `none`에서 실제 지원 수준으로 바뀐다.
  - fidelity limits는 HTML5/native/editor DnD 미지원 한계를 계속 설명한다.
  - unsupported DnD 범위와 synthetic pointer drag 지원 범위가 서로 모순되지 않는다.
- Test expectations:
  - `pnpm test -- tests/capability-fidelity.test.js`

### F2026-06-14-09 Debug Event Subscription API

- Status: [ ] Not started
- Briefing: Facade의 `on`/`off` API가 shell로 남아 있다. Trace collector와 runtime event를 외부 listener가 구독할 수 있는 최소 event bus를 추가한다.
- Dependencies: `actorble-facade`, `diagnostics-trace`, `scenario-runner`, `action-orchestrator`.
- Completion criteria:
  - `on(event, listener)`와 `off(event, listener)`가 lifecycle-safe하게 동작한다.
  - action/scenario/trace event 중 지원 이벤트 이름과 payload 안정성이 문서화된다.
  - `destroy()` 이후 listener 호출 또는 leak이 없다.
- Test expectations:
  - `pnpm test -- tests/actorble-facade.test.js tests/diagnostics-trace.test.js`

### F2026-06-14-10 Implementation Task Document Reconciliation

- Status: [ ] Not started
- Briefing: `docs/implementation_tasks.md`는 T18-T40을 completed로 표시하지만, public API shell과 smoke 실패 같은 남은 작업도 같은 문서 상단에 적고 있다. 기존 완료 문서와 이 후속 작업 문서를 정합성 있게 정리한다.
- Dependencies: 완료된 F2026-06-14-01A와 F2026-06-14-01B의 최신 smoke 결과, public API 구현 범위 결정.
- Completion criteria:
  - `implementation_tasks.md`는 완료된 vertical slice와 남은 follow-up 범위를 혼동하지 않는다.
  - 이 문서의 완료된 작업은 상태를 갱신하거나 원 문서의 새 T41+ 항목으로 승격한다.
  - 검증 명령 결과가 문서의 상태와 일치한다.
- Test expectations:
  - 문서 변경만 있으면 별도 테스트는 필요 없다.
  - 상태 변경이 구현 변경과 함께 들어가면 해당 작업의 verification command를 함께 실행한다.

## 첫 후속 vertical slice

가장 먼저 확인할 slice는 browser smoke 복구다.

```txt
example task scenario
-> createActorble({ visual: true })
-> typeInto(..., { focusStrategy: 'click' })
-> click()
-> waitFor({ kind: 'custom' })
-> visual overlay hit-test check
-> smoke pass
```

이 slice가 통과하면 현재 예제가 실제 browser runtime에서 public API만으로 동작한다는 신호가 된다. 그 다음에는 F2026-06-14-02A/B로 facade shell과 unsupported reporting을 정리하고, F2026-06-14-03A-D로 `focus/type/fill/press`를 각각 public path에 연결한다.

## 실행 체크리스트

- 새 동작은 실패하는 Vitest 또는 smoke assertion을 먼저 추가한다.
- 기존 사용자 변경이 있는 파일은 되돌리지 않는다.
- public API shell 제거 시 facade, orchestrator, engine boundary 테스트를 함께 갱신한다.
- browser 한계는 silent fallback보다 capability/fidelity 또는 `PLATFORM_UNSUPPORTED`로 드러낸다.
- 각 작업 완료 시 가능한 범위에서 `pnpm test`, `pnpm typecheck`, `pnpm build`를 실행한다.
