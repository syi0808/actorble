# Shared Browser Internals

Home for browser-package-internal primitives that are shared across multiple component boundaries.

Do not move code to a repository-level shared package until another platform needs it.
