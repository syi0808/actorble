# Browser Platform Adapter

Isolates browser APIs behind DOM, event, state, and style adapter boundaries.

This layer performs actual DOM access and event/state/style application for browser-only behavior.
