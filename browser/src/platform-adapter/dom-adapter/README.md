# DOM Adapter

Owns browser DOM reads and imperative operations such as hit testing, focus, blur, active element reads, style reads, rect reads, scroll operations, and connectedness checks.

It should ignore Actorble internal overlay elements during hit tests.
