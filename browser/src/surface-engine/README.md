# Surface Engine

Manages where interaction happens: viewport, scroll containers, iframes, shadow roots, dialogs, popovers, modals, fixed layers, reveal behavior, and coordinate-space mapping.

It provides surface and clipping inputs to geometry without deciding interactability.
