# Focus Engine

Requests and manages focus operations, blur, previous focus tracking, focus-visible modality, typing preconditions, and Tab navigation behavior.

The source of truth for focus remains platform focus state, synchronized back into the Interaction State Store.
