# Pseudo State Mirror

Best-effort visual fidelity layer for mirroring pseudo states such as hover, active, and focus-visible through runtime attributes and injected styles.

Mirror failures should produce diagnostics warnings, not action failures.
