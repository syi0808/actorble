# Wait / Observation Engine

Waits for UI conditions and settlement: visibility, hidden state, text, focus changes, layout stability, animation settlement, mutation quiet windows, custom conditions, and timeout handling.

It also informs cache invalidation for geometry-sensitive observations.
