# Text Input Engine

Manages text insertion, typing cadence, selection handling, beforeinput/input/change dispatch, composition/IME considerations, controlled inputs, and editor adapter integration.

It should preserve the distinction between `type`, `typeInto`, and `fill`.
