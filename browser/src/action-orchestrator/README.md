# Action Orchestrator

Owns high-level action lifecycles as transactions: resolve, reveal, geometry, preflight, perform, wait, cleanup, timeout, retry, force policy, and action trace spans.

It coordinates engines but does not own platform-specific DOM behavior directly.
