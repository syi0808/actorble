# Inspector

Owns target picker launch, hover highlight coordination, locator candidate preview, match count display, and selected target transfer into a scenario document.

It does not own runtime target resolution semantics from `@actorble/browser`.

