# Side Panel Entrypoint

Primary authoring surface for scenario lists, step editing, target picker launch, locator previews, validation errors, import/export, dry runs, and trace detail.

WXT uses `sidepanel` as the entrypoint name. This maps to the `side-panel` architecture boundary.

