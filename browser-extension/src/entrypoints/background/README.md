# Background Entrypoint

Coordinates extension-level state such as tab and frame routing, command handling, storage access, content script readiness, session metadata, and permission checks.

It must not execute Actorble actions directly.

