# Builder

Owns scenario authoring session state, structured draft document edits, target slot selection, and validation write-back for the browser extension workflow builder.

Builder state is UI/session state. Stored artifacts remain portable scenario documents from `src/scenario`.
