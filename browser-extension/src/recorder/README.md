# Recorder

Owns page event capture, event-to-step normalization, and locator synthesis for browser-led draft scenario documents.

Recorder output should prefer stable intent steps such as click and fill over raw browser events.

