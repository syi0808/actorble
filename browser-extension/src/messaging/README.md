# Messaging

Owns extension message contracts and correlation metadata for scenario, recorder, inspector, trace, and runtime status channels.

Messages should carry tab, frame, scenario, and run identifiers where needed.

