# Scenario

Owns extension-side scenario document validation wiring, migration wiring, browser runtime compilation, and code export.

Portable schema files remain under `../schemas/scenario`. Browser runtime behavior remains in `@actorble/browser`.

